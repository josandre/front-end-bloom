version: 1
frontend:
  phases:
    prebuild:
      commands:
        - amplifyPush --simple
    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: dist/front-end
    files:
      - '**/*'
